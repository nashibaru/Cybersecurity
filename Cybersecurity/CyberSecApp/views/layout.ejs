<!DOCTYPE html>
<html lang="pl">
    <head>
        <meta charset="utf-8" />
        <title><%= title || 'Panel' %></title>
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body class="<%= typeof page !== 'undefined' ? page : '' %>">
        <div class="container">
            <header>
            <h1><%= title || '' %></h1>
            </header>