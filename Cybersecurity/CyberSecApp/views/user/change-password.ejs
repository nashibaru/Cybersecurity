<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zmiana Hasła</title>
    <link rel="stylesheet" href="/css/styles.css">
    <!-- Load reCAPTCHA API -->
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    <style>
        .g-recaptcha {
            margin: 1.5rem 0;
            display: flex;
            justify-content: center;
        }
        .recaptcha-info {
            background: #e8f4fd;
            padding: 10px;
            border-radius: 5px;
            margin: 10px 0;
            text-align: center;
            font-size: 0.9rem;
            color: #555;
        }
        .alert-success {
            background-color: #d4edda;
            border-color: #c3e6cb;
            color: #155724;
            padding: 10px;
            border-radius: 5px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Zmiana Hasła</h1>
            <% if (user) { %>
                <small class="user-info">
                    Zalogowany jako: <strong><%= user.username %></strong> (<%= user.role %>)
                </small>
            <% } %>
        </header>

        <main>
            <div class="page-header">
                <h2>Zmiana hasła</h2>
            </div>

            <% if (message) { %>
                <% if (message.startsWith('success:')) { %>
                    <div class="alert-success">
                        <%= message.substring(8) %>
                    </div>
                <% } else { %>
                    <div class="alert alert-error">
                        <%= message %>
                    </div>
                <% } %>
            <% } %>

            <% if (mustChange) { %>
                <div class="alert alert-warning">
                    <strong>Wymagana zmiana hasła!</strong><br>
                    System wymaga zmiany hasła (pierwsze logowanie lub hasło wygasło).
                </div>
            <% } %>

            <nav class="nav-links">
                <% if (user && user.role === 'ADMIN') { %>
                    <a href="/admin/settings">Ustawienia</a>
                    <a href="/admin/users">Użytkownicy</a>
                    <a href="/admin/logs">Logi</a>
                <% } %>
                <a href="/user/change-password">Zmiana hasła</a>
                <a href="/logout">Wyloguj</a>
            </nav>

            <div class="card">
                <div class="card-header">
                    Formularz zmiany hasła
                </div>
                <div class="card-body">
                    <form method="post" action="/user/change-password" id="passwordForm">
                        <div class="form-group">
                            <label for="old_password">Aktualne hasło *</label>
                            <input type="password" id="old_password" name="old_password" 
                                   class="form-control" required 
                                   placeholder="Wprowadź aktualne hasło">
                        </div>

                        <div class="form-group">
                            <label for="new_password">Nowe hasło *</label>
                            <input type="password" id="new_password" name="new_password" 
                                   class="form-control" required 
                                   placeholder="Wprowadź nowe hasło">
                            <small class="form-text text-muted">
                                Hasło musi mieć co najmniej 8 znaków, w tym wielką literę, małą literę, cyfrę i znak specjalny.
                            </small>
                        </div>

                        <div class="form-group">
                            <label for="new_password2">Powtórz nowe hasło *</label>
                            <input type="password" id="new_password2" name="new_password2" 
                                   class="form-control" required 
                                   placeholder="Powtórz nowe hasło">
                        </div>

                        <!-- reCAPTCHA Section -->
                        <div class="recaptcha-info">
                            🔒 Aby chronić Twoje konto, prosimy o potwierdzenie, że nie jesteś robotem.
                        </div>

                        <div class="g-recaptcha" 
                             data-sitekey="<%= recaptchaSiteKey %>"
                             data-callback="onRecaptchaSuccess"
                             data-expired-callback="onRecaptchaExpired"
                             data-error-callback="onRecaptchaError">
                        </div>

                        <div id="recaptchaError" class="alert alert-error" style="display: none;">
                            Proszę potwierdzić, że nie jesteś robotem.
                        </div>

                        <button type="submit" class="btn btn-success" id="submitButton" disabled>
                            🔐 Zmień hasło
                        </button>
                    </form>
                </div>
            </div>
            <!-- License Activation Section -->
            <div class="card mt-4">
                <div class="card-header">
                    🚀 Aktywacja Edycji Plików
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <strong>Informacja:</strong> 
                        Masz <strong><%= typeof remainingUses !== 'undefined' ? (remainingUses === 'unlimited' ? 'nieograniczony' : remainingUses) : user.free_uses || 5 %></strong> 
                        <%= typeof remainingUses !== 'undefined' && remainingUses === 'unlimited' ? 'dostęp' : (remainingUses === 1 ? 'dostęp' : 'dostępy') %> do funkcji premium.
                    </div>

                    <form method="post" action="/user/activate-license">
                        <div class="form-group">
                            <label for="encrypted_key">Zaszyfrowany Klucz Licencyjny *</label>
                            <input type="text" id="encrypted_key" name="encrypted_key" 
                                class="form-control" required 
                                placeholder="Wprowadź zaszyfrowany klucz (tylko litery)">
                        </div>

                        <div class="form-group">
                            <label for="shift_key">Klucz Przesunięcia *</label>
                            <input type="number" id="shift_key" name="shift_key" 
                                class="form-control" required min="1" max="25"
                                placeholder="Wprowadź klucz przesunięcia (1-25)">
                        </div>

                        <button type="submit" class="btn btn-warning">
                            🗝️ Aktywuj Licencję
                        </button>
                    </form>

                    <div class="mt-3">
                        <a href="/user/file-edit" class="btn btn-success">
                            🎯 Przejdź do Funkcji Premium
                        </a>
                    </div>
                </div>
            </div>
        </main>

        <footer style="text-align: center; padding: 2rem; color: #6c757d; border-top: 1px solid #dee2e6;">
            <p>&copy; <%= new Date().getFullYear() %> System Bezpieczeństwa. Wszelkie prawa zastrzeżone.</p>
        </footer>
    </div>

    <!-- External JavaScript files -->
    <script src="/js/password-strength.js"></script>
    <script src="/js/change-password.js"></script>
</body>
</html>